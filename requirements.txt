# Runtime-only dependencies for the deployed API (used by Vercel's Python
# builder). scikit-learn, pyarrow, tqdm and the dev tools in pyproject.toml's
# [project.optional-dependencies] are training-pipeline-only and deliberately
# excluded here to keep the serverless function small.
fastapi>=0.111
pydantic>=2.7
pydantic-settings>=2.3
pandas>=2.2
numpy>=1.26
lightgbm>=4.3
httpx>=0.27
tenacity>=8.3
python-multipart>=0.0.9
