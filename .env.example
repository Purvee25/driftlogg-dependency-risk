# Personal access token — raises the GitHub API limit from 60 to 5000 req/hour.
# Create at https://github.com/settings/tokens with public_repo scope only.
DRIFTLOGG_GITHUB_TOKEN=

# Which registry to study. Pick one and stay there.
DRIFTLOGG_ECOSYSTEM=npm
